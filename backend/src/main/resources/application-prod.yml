spring:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

logging:
  level:
    root: WARN
    in.ac.iiitb.ca: INFO

management:
  endpoint:
    health:
      show-details: never
